# ✅ Чек-лист редактирования СТРАТЕГОС

Используй этот чек-лист при каждом редактировании контента.

---

## 🔍 Перед редактированием

- [ ] Открыл DevTools консоль (F12)
- [ ] Нашёл нужный файл (js/s2_more_p*.js или js/s2_*.js)
- [ ] Понимаю структуру данных (какой тип вопроса редактирую)
- [ ] Сделал бэкап (git status, git diff)

---

## ✏️ Редактирование вопроса

### Часть 1A/1B (множественный выбор)

**Что редактирую:**
- [ ] Текст вопроса (`q:`)
- [ ] Варианты ответов (`o:`)
- [ ] Правильный ответ (`a:` — индекс 0–3)
- [ ] Объяснение дистракторов (`trap:`)
- [ ] Авторская атрибуция (`authors:`)

**Проверка:**
```javascript
// В консоли после refresh
const q = ALL2().find(x => x.id === "q-ххх");
console.log(q.q);  // должен быть твой текст
console.log(q.o);  // варианты
console.log(q.a);  // индекс ответа
```

**Типичная ошибка:** забыли обновить `a` при перестановке вариантов
```javascript
// ❌ НЕПРАВИЛЬНО: вариант переставили, но индекс не изменили
o: ["B", "A", "C", "D"], a: 0  // A теперь на индексе 1!

// ✅ ПРАВИЛЬНО:
o: ["B", "A", "C", "D"], a: 1  // A на индексе 1
```

---

### Часть 3 (впиши понятие)

**Что редактирую:**
- [ ] Текст вопроса (`q:`)
- [ ] Эталонные ответы (`a:` — массив синонимов)
- [ ] Подсказка (`hint:`)
- [ ] Связь с концептом (`concept:`)

**Проверка:**
```javascript
const q = ALL2().find(x => x.id === "q-ххх");
console.log(q.a);  // массив: ["стратегия", "стратег. позиция", ...]

// Тест fuzzy-match
fillOk("стратeгия", q.a);  // true (ё→е, опечатка)
fillOk("ОЭ", q.a);         // false (неправильно)
```

**Типичная ошибка:** забыли добавить синонимы
```javascript
// ❌ НЕПРАВИЛЬНО: только один вариант
a: ["стратегия"]

// ✅ ПРАВИЛЬНО: эквиваленты и синонимы
a: ["стратегия", "стратегическая позиция", "уникальная позиция"]
```

---

### Часть 4 (открытый кейс)

**Что редактирую:**
- [ ] Ситуация кейса (`q:`)
- [ ] Рубрика (критерии проверки) (`rubric:` — 4 пункта)
- [ ] Эталонный ответ (`model:`)
- [ ] Компания (`case_id:`)

**Проверка:**
```javascript
const q = ALL2().find(x => x.id === "case-ххх");
console.log(q.rubric.length);  // должно быть ровно 4
console.log(q.model.length);   // должен быть развёрнут
```

**Типичная ошибка:** рубрика содержит <4 пунктов
```javascript
// ❌ НЕПРАВИЛЬНО: только 3 критерия
rubric: ["1. ...", "2. ...", "3. ..."]

// ✅ ПРАВИЛЬНО: ровно 4 критерия
rubric: ["1. ...", "2. ...", "3. ...", "4. ..."]
```

---

## 📝 Добавление нового вопроса

### Шаг 1: Выбрать тему и часть

- [ ] Какая тема? (t1–t12)
- [ ] Какая часть? (1A, 1B, 3, 4)
- [ ] В какой файл положить?
  - Часть 1A (темы 1–6) → s2_more_p1a.js
  - Часть 1A (темы 7–12) → s2_more_p1b.js
  - Часть 1B → s2_more_p2.js
  - Часть 3 → s2_more_p3.js
  - Часть 4 → s2_more_p4.js

### Шаг 2: Проверить уникальность ID

```javascript
// В консоли
ALL2().map(q => q.id).includes("q-xyz")  // false = хорошо
```

- [ ] ID не повторяется в банке

### Шаг 3: Скопировать шаблон фабрики

```javascript
// Часть 1A
_m1("q-new-001", "t1", "Вопрос?", ["А", "Б", "В", "Г"], 1, "почему неверна")

// Часть 1B
_m2("q-new-002", "t1", "Вопрос?", [{text: "А", desc: "описание"}, ...], [0, 2], "trap")

// Часть 3
_m3("q-new-003", "t1", "Вопрос?", ["эталон", "синоним"], "подсказка", "c-concept", [])

// Часть 4
_m4("q-new-004", "t1", "Кейс...", ["Критерий 1", "Критерий 2", "Критерий 3", "Критерий 4"], "Эталонный ответ", "company_id")
```

- [ ] Выбрал правильную фабрику
- [ ] Заполнил все параметры

### Шаг 4: Добавить в S2.BANK или S2.DEMO

В конце файла найти строку `S2.BANK.push(...)` и добавить новый вопрос, или:

```javascript
// В конце файла
S2.BANK.push(_m1(
  "q-new-001",
  "t1",
  "Мой новый вопрос?",
  ["Вариант А", "Вариант Б", "Вариант В", "Вариант Г"],
  2,  // правильный ответ = В (индекс 2)
  "Ловушка: не путайте с X"
));
```

- [ ] Вопрос добавлен в S2.BANK (или S2.DEMO)

### Шаг 5: Проверить в консоли

```javascript
location.reload();  // обновить
const q = ALL2().find(x => x.id === "q-new-001");
console.log(q);     // должен быть новый вопрос
```

- [ ] Вопрос появился в ALL2()
- [ ] Все параметры корректны

---

## 🎨 Редактирование концепта/темы

**Файл:** js/s2_themes.js

**Структура:**
```javascript
{
  id: "t1",
  title: "Суть стратегии",
  concepts: [
    {
      id: "c-strategy",
      name: "Стратегия",
      essence: "**это** уникальная позиция на рынке",  // для активного вспоминания
      hook: "🧲 Крючок памяти: отличается от ОЭ",
      authors: ["Портер"],
      definition: "Полное определение...",
      ...
    }
  ]
}
```

**Что редактирую:**
- [ ] `name:` — название концепта
- [ ] `essence:` — суть одной строкой (для активного вспоминания)
- [ ] `hook:` — крючок памяти 🧲 (как запомнить)
- [ ] `authors:` — авторы (для дрилла атрибуции)
- [ ] `definition:`, `deep:` и т.д. — полное описание

**Проверка:**
```javascript
const t = S2.THEMES.find(x => x.id === "t1");
const c = t.concepts.find(x => x.id === "c-strategy");
console.log(c.name);      // должно совпадать
console.log(c.essence);   // **жирный**, *курсив* OK
```

---

## 🎯 Редактирование trap-drill

**Файл:** js/s2_traps.js

**Структура:**
```javascript
{
  id: "trap-001",
  left: {
    name: "Стратегия",
    def: "Уникальная позиция..."
  },
  right: {
    name: "ОЭ",
    def: "Делать лучше то же..."
  },
  rule: "Как не перепутать: **Стратегия** про позицию, **ОЭ** про методы",
  example: "Пример: Southwest (стратегия) vs United Airlines (ОЭ)"
}
```

**Что редактирую:**
- [ ] `left.name:` и `right.name:` — два понятия
- [ ] `left.def:` и `right.def:` — их определения
- [ ] `rule:` — правило как не перепутать
- [ ] `example:` — конкретный пример

**Проверка:**
```javascript
const trap = S2.TRAPS.find(x => x.id === "trap-001");
console.log(trap.left.name);  // должно быть заполнено
console.log(trap.right.name); // должно быть заполнено
```

---

## ⚠️ Проверка перед сохранением

- [ ] **Синтаксис JS:** нет ошибок в консоли (F12)
- [ ] **Кириллица:** текст на русском отображается корректно
- [ ] **ID уникальны:** `ALL2().map(q => q.id)` не содержит дублей
- [ ] **Индексы верны:** `a: 0/1/2/3` для одиночных, `[...]` для множественных
- [ ] **Массивы замкнуты:** `[]` и `{}` не забыл закрыть
- [ ] **Заполнены обязательные поля:** q, o, a (минимум)

---

## 💾 После редактирования

### Локально

1. [ ] Сохранил файл (Ctrl+S)
2. [ ] Hard refresh браузера (Ctrl+Shift+R)
3. [ ] Проверил в консоли: `ALL2().find(q => q.id === "...")`
4. [ ] Тестировал вопрос на сайте (ответить, проверить)
5. [ ] Очистил localStorage если нужно: `localStorage.removeItem("strategos_v1")`
6. [ ] Перезагрузился: `location.reload()`

### Перед пушем в Git

1. [ ] `git status` — посмотрел что изменилось
2. [ ] `git diff js/s2_more_*.js` — проверил diff
3. [ ] Консоль не содержит `console.log` отладочных строк
4. [ ] Версия в index.html обновлена (меняется при каждом релизе)

```html
<script>window.__APP_VER="2026-06-18";</script>
```

5. [ ] `git add .`
6. [ ] `git commit -m "Описание: что изменилось"`
7. [ ] `git push origin main`

### На Netlify

- [ ] Проверил что деплой прошёл (5–30 сек)
- [ ] Открыл сайт на Netlify: https://monumental-concha-bcaa3d.netlify.app
- [ ] Hard refresh (Ctrl+Shift+R)
- [ ] Проверил что изменения видны на сайте

---

## 🔥 Часто встречающиеся ошибки

| Ошибка | Как исправить |
|--------|---------------|
| "Не вижу новый вопрос" | Hard refresh Ctrl+Shift+R |
| "Вопрос не проходит" | Проверить `a:` индекс |
| "ReferenceError: _m1 is not defined" | Убедиться что функция в одном файле |
| "Синтаксис ошибка" | DevTools → Console, найти строку |
| "Старые данные на Netlify" | Дождаться 5–30 сек, Ctrl+Shift+R |
| "localStorage не обновляется" | `localStorage.removeItem("strategos_v1")` |

---

## 📞 Когда нужна помощь

Свяжись со мной если:
- [ ] Не можешь найти файл
- [ ] Не понимаешь структуру вопроса
- [ ] Синтаксис JS вызывает трудность
- [ ] На Netlify не появляется после пуша

---

**Готово к редактированию! 🎯**
